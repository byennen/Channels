#flash_message
  - flash.each do |type, message|
    %div.alert.fade.in.animated.shake{ "data-alert" => "alert", :class => "alert-#{twitterized_type(type)}" }
      %a.close{"data-dismiss" => "alert"} ×
      %p
        = message
