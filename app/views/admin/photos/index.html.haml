%h1 Photos
%table.table.table-striped
  %thead
    %tr
      %th Photo
      %th Name
      %th Paid?
      %th Active?
      %th
      %th
  %tbody
    - @photos.each do |photo|
      %tr
        %td= image_tag photo.image.thumb('50x50#').url if photo.image_uid
        %td= photo.title
        %td= photo.paid
        %td
          %a.btn{:href => edit_admin_channel_photo_path(@channel, photo)}
            %i.icon-pencil
            Edit
          %a.btn.btn-danger{:href => admin_channel_photo_path(@channel, photo), :'data-method' => :delete, :'data-confirm' => t('web-app-theme.confirm', :default => 'Are you sure?')}
            %i.icon-trash.icon-white
            Destroy
%a.btn.btn-primary{:href => new_admin_channel_photo_path}
  %i.icon-plus-sign.icon-white
  New Photo
